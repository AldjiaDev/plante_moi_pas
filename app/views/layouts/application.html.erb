<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Plante Moi Pas" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Icônes et manifest -->
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Font Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
    </style>

    <!-- Tailwind + App CSS -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-plante-clair min-h-screen text-gray-800 font-sans antialiased">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-6">

      <!-- ✅ Mini Header -->
      <header class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-bold text-plante">🌿 Plante-moi pas</h1>

        <% if user_signed_in? %>
          <div class="flex items-center gap-3">
            <%= link_to "Profil", profile_path, class: "text-sm text-gray-600 hover:text-green-700" %>
            <%= link_to "Déconnexion", destroy_user_session_path, method: :delete,
                  class: "text-sm bg-plante hover:bg-plante-fonce text-white font-medium py-1 px-3 rounded" %>
          </div>
        <% end %>
      </header>

      <!-- ✅ Flash messages -->
      <% if notice %>
        <div class="bg-green-100 text-green-800 p-3 rounded-xl mb-4 border border-green-300">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 text-red-800 p-3 rounded-xl mb-4 border border-red-300">
          <%= alert %>
        </div>
      <% end %>

      <!-- ✅ Contenu principal -->
      <%= yield %>
    </div>
  </body>
</html>
