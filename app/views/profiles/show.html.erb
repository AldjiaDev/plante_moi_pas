<div class="bg-white rounded-2xl shadow-md p-6 border border-gray-200">
  <h2 class="text-2xl font-bold text-green-700 mb-4">👤 Mon profil</h2>

  <p class="mb-2"><strong>Email :</strong> <%= @user.email %></p>
  <p class="mb-2"><strong>Succès débloqués :</strong> <%= @achievement_count %></p>

  <% if @plant %>
    <p class="mb-2"><strong>Ma plante :</strong> <%= @plant.name %> (<%= @plant.personality %>)</p>
    <p class="text-sm italic text-gray-600">Dernier arrosage : <%= @plant.last_watered_at&.to_date || "jamais" %></p>
    <%= link_to "Voir ma plante", plant_path, class: "inline-block mt-4 bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded" %>
  <% else %>
    <p>Tu n’as pas encore de plante 🌱</p>
    <%= link_to "Créer une plante", new_plant_path, class: "inline-block mt-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded" %>
  <% end %>
</div>
