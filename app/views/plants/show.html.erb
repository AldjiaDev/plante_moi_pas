<h2>🎯 Quête du jour :</h2>

<p><strong><%= @quest.title %></strong></p>
<p><%= @quest.description %></p>

<% if @log.quest_done? %>
  <p>✅ Quête déjà complétée</p>
  <% if @log.quest_response.present? %>
    <p><em>Ta réponse : <%= @log.quest_response %></em></p>
  <% end %>
<% elsif @quest.quest_type == "text_input" %>
  <%= form_with url: submit_quest_response_path, method: :post do |form| %>
    <div>
      <%= form.label :quest_response, "Ta réponse :" %><br>
      <%= form.text_area :quest_response, rows: 3 %>
    </div>
    <%= form.submit "Envoyer" %>
  <% end %>
<% else %>
  <%= button_to "Faire la quête", quest_plant_path, method: :post %>
<% end %>

<hr>

<h2>🌿 État de ta plante :</h2>

<!-- 🗣️ Message d’humeur selon la personnalité -->
<p class="italic text-purple-700 text-lg mb-4">
  <%= @plant.mood_phrase %>
</p>


<!-- 🌱 Visuel selon le stade -->
<% case @plant.growth_stage %>
<% when "sprout" %>
  <p style="font-size: 4rem;">🌱</p>
  <p>Je viens de naître... ne m'oublie pas !</p>
<% when "leafy" %>
  <p style="font-size: 4rem;">🌿</p>
  <p>Regarde comme je deviens feuillu 🌿</p>
<% when "bushy" %>
  <p style="font-size: 4rem;">🌴</p>
  <p>Je suis touffue et fière 😎</p>
<% when "tree" %>
  <p style="font-size: 4rem;">🌳</p>
  <p>Je suis devenue un arbre majestueux. Arrose-moi comme une reine 👑</p>
<% else %>
  <p style="font-size: 4rem;">🪴</p>
  <p>État inconnu… ai-je été clonée ? 🤖</p>
<% end %>

<hr>

<h2>🏆 Succès débloqués :</h2>

<% if @unlocked_achievements.any? %>
  <ul>
    <% @unlocked_achievements.each do |achievement| %>
      <li>
        <strong><%= achievement.name %></strong> — <%= achievement.description %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Aucun succès encore… mais ça viendra 😏</p>
<% end %>
