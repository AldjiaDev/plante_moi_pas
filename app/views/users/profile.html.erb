<div class="container-gap">
  <div class="nes-container with-title is-rounded">
    <p class="title">👤 Mon Profil</p>

    <p><strong>Nom :</strong> <%= current_user.name || "Utilisateur anonyme" %></p>
    <p><strong>Email :</strong> <%= current_user.email %></p>

    <% if current_user.plant.present? %>
      <p><strong>🌿 Ma plante :</strong> <%= current_user.plant.name %></p>
    <% else %>
      <p>Tu n'as pas encore de plante. <%= link_to "Créer une plante", new_plant_path, class: "nes-btn is-primary is-small ml-2" %></p>
    <% end %>
  </div>

  <div class="text-center">
    <%= link_to "⬅️ Retour à ma plante", plant_path, class: "nes-btn is-warning" %>
  </div>
</div>
