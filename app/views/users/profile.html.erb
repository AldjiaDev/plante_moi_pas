<div class="bg-white p-6 rounded-2xl shadow-md border border-gray-200 max-w-xl mx-auto mt-8">
  <h1 class="text-2xl font-bold text-green-700 mb-4">👤 Mon profil</h1>

  <div class="mb-6">
    <p class="text-gray-600"><strong>Email :</strong> <%= current_user.email %></p>
    <p class="text-gray-600 mt-1"><strong>Succès débloqués :</strong> <%= current_user.achievements.count %></p>
  </div>

  <% if current_user.plant %>
    <div class="bg-green-50 p-4 rounded-xl border border-green-200 shadow-sm">
      <h2 class="text-lg font-semibold text-green-700 mb-2">🌿 Ma plante</h2>
      <p><strong>Nom :</strong> <%= current_user.plant.name %></p>
      <p><strong>Personnalité :</strong> <%= current_user.plant.personality.capitalize %></p>
      <p><strong>Feuilles :</strong> <%= current_user.plant.leaves %></p>
      <p><strong>Humeur actuelle :</strong> <%= current_user.plant.mood %></p>
    </div>
  <% else %>
    <div class="mt-4">
      <p class="text-gray-600 italic">Tu n’as pas encore de plante…</p>
      <%= link_to "Créer ma plante 🌱", new_plant_path, class: "inline-block mt-2 bg-green-600 text-white py-2 px-4 rounded hover:bg-green-700" %>
    </div>
  <% end %>
</div>
